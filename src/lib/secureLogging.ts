/**
 * @deprecated Import from "../middlewares/secureLogging.js" instead.
 * This file is a re-export barrel for backward compatibility.
 */
export {
  sanitizeLogMessage,
  secureError,
  secureLog,
  secureWarn,
  extractSafeMetadata,
  SecureLogger,
  secureRequestLogger,
} from "../middlewares/secureLogging.js";
